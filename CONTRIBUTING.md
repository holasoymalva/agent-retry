# Contributing

Use Node.js 22+ and pnpm. Run `pnpm build`, `pnpm test`, `pnpm lint`, and `pnpm typecheck` before
submitting a change. Keep provider dependencies outside `@agent-retry/core`, add deterministic tests
for recovery paths, and preserve bounded execution.
